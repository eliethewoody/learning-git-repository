(defn pearson
    [xs ys]
    (if (= (count xs) (count ys))
        (let [ax (/ (apply + xs) (count xs))
              ys (/ (apply + ys) (count ys))]
            (loop [x (count xs)]
                (when (> x 1)
                    
            (recur (- x 1))))        

            


;;ЖРИ ГОВНО, СУКА!


